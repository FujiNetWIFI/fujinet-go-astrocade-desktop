# Flatpak manifest for the GNOME frontend.
#
# Self-contained: every input is declared here, so this builds from a plain
# checkout with no preparation steps -- which is also what makes it work
# unchanged in GNOME Builder.
#
# The app source is the local tree minus the directories the manifest
# provides itself: third_party/fujinet-firmware comes from the pinned commit
# below (the same astrocade-bringup commit as cmake/Dependencies.cmake), so a
# flatpak build never depends on a working checkout's state.
#
# Build + install + run:
#   flatpak-builder --user --install --force-clean build-flatpak \
#       build-aux/flatpak/online.fujinet.go.astrocade.gnome.yml
#   flatpak run online.fujinet.go.astrocade.gnome
#
# NOTE: the bundled FujiNet runtime (mbedTLS + the python webui deps + the
# WITH_FUJINET build option) lands with the FujiNet milestone; until then this
# builds SDL3 + the app, and FujiNet runs link-down like a standalone
# fujinet-pc that is not started.

app-id: online.fujinet.go.astrocade.gnome
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: fujinet-go-astrocade-gnome

finish-args:
  - --share=ipc
  - --share=network          # the FujiNet BoIP link + the loopback web UI
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio      # SDL audio (PipeWire's Pulse interface)
  - --device=all             # gamepad hotplug (hidraw/evdev)

modules:
  - name: sdl3
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DSDL_STATIC=OFF
      - -DSDL_TEST_LIBRARY=OFF
    sources:
      - type: git
        url: https://github.com/libsdl-org/SDL.git
        # keep in step with the SDL the CI jobs build
        tag: release-3.4.12
        commit: f87239e71e42da91ca317a12eefb82cfbf3393eb

  # The FujiNet runtime's vendored build_webui.py imports PyYAML and Jinja2
  # from whatever python3 tools/fujinet/build-fujinet-desktop.sh runs --
  # every CI job's runner already has both system-wide, but org.gnome.Sdk's
  # python3 has neither and the sandboxed build step can't pip install them
  # (no network). Installed to a fixed, python-version-independent path so
  # the fujinet-go-astrocade module below can just PYTHONPATH at it.
  - name: python3-fujinet-webui-deps
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --no-index --find-links="file://${PWD}"
        --target=/app/fujinet-pyshims PyYAML==6.0.3 MarkupSafe==3.0.3 Jinja2==3.1.6
    sources:
      - type: file
        url: https://files.pythonhosted.org/packages/05/8e/961c0007c59b8dd7729d542c61a4d537767a59645b82a0b521206e1e25c2/pyyaml-6.0.3.tar.gz
        sha256: d76623373421df22fb4cf8817020cbb7ef15c725b9d5e45f17e189bfc384190f
      - type: file
        url: https://files.pythonhosted.org/packages/7e/99/7690b6d4034fffd95959cbe0c02de8deb3098cc577c67bb6a24fe5d7caa7/markupsafe-3.0.3.tar.gz
        sha256: 722695808f4b6457b320fdc131280796bdceb04ab50fe1795cd540799ebe1698
      - type: file
        url: https://files.pythonhosted.org/packages/df/bf/f7da0350254c0ed7c72f3e33cef02e048281fec7ecec5f032d4aac52226b/jinja2-3.1.6.tar.gz
        sha256: 0137fb05990d35f1275a587e9aee6d56da821fc83491a0fb838183be43f66d6d

  - name: fujinet-go-astrocade
    buildsystem: cmake-ninja
    builddir: true
    build-options:
      env:
        PYTHONPATH: /app/fujinet-pyshims
    config-opts:
      - -DFRONTEND=gnome
      # The Bally system ROMs are copyrighted firmware with no
      # redistributable substitute -- see COMPLIANCE.md. Every published
      # artifact, this flatpak included, embeds none and leaves it to the
      # app's own "Import System ROM" path.
      - -DWITH_ASTROCADE_ROMS=OFF
    sources:
      - type: dir
        path: ../..
        skip:
          - .flatpak-builder
          - build
          - build-flatpak
          - build-flatpak-kde
          - third_party
          - tools/fujinet/work
      # Pinned exactly as cmake/Dependencies.cmake: the astrocade-bringup
      # commit. astro_provide_dependency() skips its own fetch once it finds
      # build.sh (the SENTINEL) already here.
      - type: git
        url: https://github.com/FujiNetWIFI/fujinet-firmware
        commit: 20e89ead5d88e8c441b448d231d13ca63aa71dc5
        dest: third_party/fujinet-firmware
      # Same reasoning: the sandboxed build step has no network access, but
      # tools/fujinet/build-fujinet-desktop.sh needs mbedTLS 3.x and
      # org.gnome.Sdk ships none. Pinned to the exact tag/commit the script
      # itself builds when it has to fetch mbedTLS live (ensure_mbedtls's
      # MBEDTLS_TAG) -- ensure_mbedtls skips that fetch once it finds a
      # checkout already here.
      - type: git
        url: https://github.com/Mbed-TLS/mbedtls.git
        tag: mbedtls-3.6.5
        commit: e185d7fd85499c8ce5ca2a54f5cf8fe7dbe3f8df
        dest: tools/fujinet/work/mbedtls-src
